#!/usr/bin/with-contenv bashio
# shellcheck shell=bash
set -e

bashio::log.info "starting Duplicacy server"
#umask "${UMASK}"

HOME="${APP_DIR}" exec s6-setuidgid -u ${PUID} -g ${PGID} "${APP_DIR}/duplicacy_web"
